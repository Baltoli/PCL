external @stdio

fresh Input {
    (
        in Input(Num).
        in Input(Mod).
        in Input(Cont).
        out Cont(Num - ((Num / Mod) * Mod))
    |
        in @stdio(N).
        in @stdio(M).
        out Input(N).
        out Input(M).
        out Input(@stdio)
    )
}
