external @stdio

fresh C {
    let Pass = @secret {
        (
            !(
                in C(Y).
                in @stdio(Input).
                [Input = Pass] {
                    out @stdio(@welcome).
                    end
                }.
                out C(0)
            )
        |
            out C(0)
        )
    }
}
